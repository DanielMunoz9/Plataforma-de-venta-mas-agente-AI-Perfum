# OpenAI API Key (do NOT commit a real key)
OPENAI_API_KEY=sk-proj-REEMPLAZA_CON_TU_KEY

# Provider/model configuration
AI_PROVIDER=openai
AI_MODEL=gpt-5-mini
TEMPERATURE=0.9
TOP_P=0.95
MAX_TOKENS=500

# CORS origins allowed (comma-separated). In prod, set to your domain(s)
ALLOW_ORIGINS=*